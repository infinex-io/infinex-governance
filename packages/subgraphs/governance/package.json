{
  "name": "test-graph",
  "version": "0.1.0",
  "scripts": {
    "build": "graph codegen && graph build",
    "deploy": "graph deploy --network base-sepolia --studio dev-infinex-governance"
  },
  "devDependencies": {
    "@graphprotocol/graph-ts": "0.34.0"
  },
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "babel-register": "^6.26.0"
  }
}
